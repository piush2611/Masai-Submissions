function split_char(str) {
    var arr = [];
    for (var i = 0; i < str.length; i++) {
        arr.push(str[i]);
    }
    console.log(arr);
    return arr;
}

split_char("randomstring");

var val = split_char("randomstring")